Run Imapfilter from Emacs
=========================

This package provides the command [`imapfilter`], which runs
the `imapfilter` executable, showing progress in a buffer.

[`imapfilter`]: https://github.com/lefcha/imapfilter
